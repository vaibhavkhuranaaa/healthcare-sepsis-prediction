# Healthcare Sepsis Prediction agent contract

Read `docs/STATE.md`, `docs/HANDOFF.md`, and fresh `graphify-out/` context before broad exploration. Inspect source directly when the graph does not cover the relevant files.

Never commit MIMIC data, PHI, patient-level derivatives, credentials, or restricted research artifacts. Only the synthetic surface is deployable. Do not claim clinical-device status, care suitability, model performance, or deployment without versioned evidence; evaluation must cover calibration, leakage, alert burden, and limitations.

Preserve unrelated dirty work. Use purpose branches, conventional commits, and the configured human identity only; never add AI/model author or co-author attribution. Deployment, publication, merging, public visibility, and paid resources are separate owner-gated actions.

The project remains `building`; keep local credentialed research separated from the synthetic demo. Delegation is optional and must be bounded.
